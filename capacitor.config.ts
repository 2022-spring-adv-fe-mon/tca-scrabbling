import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tca-scrabbling',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
